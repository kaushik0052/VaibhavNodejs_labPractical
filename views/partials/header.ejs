<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <a class="navbar-brand" href="/">Fitness Tracker</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#mainNavbar"
      aria-controls="mainNavbar"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if (currentUser) { %>
        <li class="nav-item">
          <a class="nav-link" href="/workouts">My Workouts</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/workout/new">New Workout</a>
        </li>
        <% } %>
      </ul>
      <div class="d-flex align-items-center">
        <% if (currentUser) { %>
        <span class="text-white me-3">Goal: <strong><%= fitnessGoal %></strong></span>
        <a class="btn btn-outline-light btn-sm" href="/logout">Logout</a>
        <% } else { %>
        <a class="btn btn-outline-light btn-sm me-2" href="/login">Login</a>
        <a class="btn btn-outline-light btn-sm" href="/register">Register</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
